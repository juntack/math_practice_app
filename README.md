# けいさん れんしゅう

小学1年生向けの、足し算・引き算の練習用Webアプリです。

## 使い方

`index.html` をブラウザで開いてください。インストールは不要です。

- 答えは0〜9の一桁です。
- 足し算のみ、引き算のみ、両方からの出題を選べます。
- 問題数（1〜100問）を設定してからスタートできます。計測時間は終了時だけ表示されます。
- 数字を入力した瞬間に、正解なら `○`、不正解なら `×` を表示します。
- 「つぎの もんだい」または Enter キーで次の問題へ進みます。
- 「せいせきを リセット」で正解数と回答済み問題数を0に戻せます。
